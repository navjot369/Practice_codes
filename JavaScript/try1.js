function calculateDaysBetweenDates(begin, end) {
    var diff = end - begin;
    var days = Math.floor(diff / 86400000);
    return days;
}

function calculateDaysBetweenDates(begin, end) {
    var diff = end - begin;
    var days = Math.floor(diff / 86400000);
    return days;
}